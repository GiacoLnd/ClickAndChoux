{% extends 'base.html.twig' %}

{% block title %}Mon Profil{% endblock %}

{% block body %}
    <div class="text-center">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    </div>
    <div class="profile-container">
        <h2 class="details-title">Panneau Administrateur</h2>
        <div class="profile-info">
            <p>Bonjour, cheffe {{ user.nickName }} !</p>
            <p>Email Admin : {{ user.email }}</p>
        </div>
        <a href="{{ path('favoris_page') }}" class="profile-link">Mes Favoris</a>
        <a href="{{ path('app_user_commandes') }}" class="profile-link">Mes Commandes</a>
    </div>
{% endblock %}
 